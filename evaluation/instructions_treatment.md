# Treatment instructions (tracker + viewer)

You may use:
- all baseline resources, PLUS
- the read-only decision viewer (backed by index.json/graph.json/artifacts.json).

No external browsing. You have 5 minutes per task.

For each task, provide:
1) rationale/tradeoff
2) at least one supporting artifact reference AND relationship type (implements/evaluated_by/supported_by/supersedes).
